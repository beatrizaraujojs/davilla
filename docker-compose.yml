services: 
  nginx: 
    image: nginx:stable-alpine 
    container_name: davilla_nginx 
    ports: 
      - "8081:80" 
    volumes: 
      - ./src:/var/www/html
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf 
    depends_on: 
      - php 
    networks: 
      - davilla_network 
 
  php: 
    build: 
      context: . 
      dockerfile: ./docker/php/Dockerfile 
    container_name: davilla_php 
    working_dir: /var/www/html 
    volumes: 
      - ./src:/var/www/html
    depends_on: 
      - mysql 
    networks: 
      - davilla_network 
 
  mysql: 
    image: mysql:8.0
    ports:
      - "3308:3306"
    container_name: davilla_mysql 
    restart: unless-stopped 
    environment: 
      MYSQL_DATABASE: davilla 
      MYSQL_USER: user 
      MYSQL_PASSWORD: user 
      MYSQL_ROOT_PASSWORD: root 
    volumes: 
      - mysql_data:/var/lib/mysql 
    networks: 
      - davilla_network 
 
networks: 
  davilla_network: 
    driver: bridge 

      
volumes: 
  mysql_data: 
